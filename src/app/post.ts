import { Contact } from './contact';
import { CONTACTS } from './mock-list';

export class post{
    post_id: number;
    user_name='';
    poster='';
    posted_time: Date = new Date();
    like:number;
}